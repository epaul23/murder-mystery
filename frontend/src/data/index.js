// Central registry — add new cases here only
// CaseBriefing.jsx never needs to change

import { case1Briefing } from './case1.js'

export const BRIEFINGS = {
  1: case1Briefing,
  // 2: case2Briefing,  ← just uncomment when ready
  // 3: case3Briefing,
}